# Spec 048 — pinned extras for custom Airflow 3.3.0 image.
# Keep apache-airflow pinned to the image version during install (official practice).
# Provider pins from constraints-3.3.0 / constraints-3.12.txt (2026-07-06).
apache-airflow-providers-standard==1.15.0
apache-airflow-providers-fab==3.7.1
duckdb==1.1.3
pandas==2.2.2
pyarrow==16.1.0
httpx==0.27.0
python-dotenv==1.0.1
