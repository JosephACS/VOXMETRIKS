# Spec 048 — pinned extras for custom Airflow 3.3.0 image (Python 3.12).
# Keep apache-airflow pinned to the image version during install (official practice).
# Runtime pins below match constraints-3.3.0 / constraints-3.12.txt (except duckdb, ELT-only).
# https://raw.githubusercontent.com/apache/airflow/constraints-3.3.0/constraints-3.12.txt
apache-airflow-providers-standard==1.15.0
apache-airflow-providers-fab==3.7.1
duckdb==1.1.3
pandas==2.1.4
pyarrow==24.0.0
httpx==0.28.1
python-dotenv==1.2.2
