# ============================================================
# VOXMETRIK_V2 - Requirements
# Python 3.12 compatible - ALL with prebuilt wheels
# NO compilation required (no Rust, no MSVC, no jemalloc)
# ============================================================

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Database - 1.x line has prebuilt wheels for Python 3.12
# DO NOT use 0.10.x → SerializationException on Python 3.12
# DO NOT use < 0.8.0 → missing wheel for cp312
duckdb==1.1.3

# Pydantic v2 - prebuilt wheels, no Rust compilation
# DO NOT use pydantic < 2.0 → incompatible with FastAPI 0.111
# DO NOT use pydantic-core < 2.18 on Python 3.12 → maturin/cargo errors
pydantic==2.7.4
pydantic-settings==2.3.4

# Environment
python-dotenv==1.0.1

# HTTP client (for PocketBase)
httpx==0.27.0

# YouTube search fallback (no Data API quota — used when key missing or exhausted)
yt-dlp==2024.12.23

# Data processing
pandas==2.2.2
pyarrow==16.1.0

# Logging extras
python-json-logger==2.0.7

# Password hashing (prebuilt wheels for Python 3.12)
bcrypt==4.1.3

# Testing (minimal suite: health, login, playlists, favorites)
pytest==8.2.2
pytest-cov==5.0.0

# Linting / formatting (quality gate)
ruff==0.6.9