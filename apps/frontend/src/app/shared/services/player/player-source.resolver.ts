/**
 * @deprecated Use `AudioResolver` from `playback-core/resolver/audio.resolver.ts`.
 * Kept for backward compatibility during Phase 3 migration.
 */
export { AudioResolver as PlayerSourceResolver } from '../../../playback-core/resolver/audio.resolver';
export type { AudioResolveCallbacks as SourceResolverCallbacks } from '../../../playback-core/resolver/audio.resolver';
export type { PlaybackEngineMode as PlaybackMode } from '../../../playback-core/resolver/audio.resolver';
