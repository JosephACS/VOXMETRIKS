/**
 * @deprecated Spec 014 Phase C — re-export adapter.
 * Canonical: `packages/analytics/dashboard/dashboard.component`.
 * Remove when no consumers remain (routes already point to packages).
 */
export { DashboardComponent } from '../../packages/analytics/dashboard/dashboard.component';
