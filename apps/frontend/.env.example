# Frontend local environment (placeholders only)
# Angular reads apiUrl from src/environments/environment.ts by default.
# Keep this file in sync when documenting local overrides.

# Maps to environment.apiUrl
API_URL=http://127.0.0.1:8000/api/v1

# Optional documentation aliases (not loaded unless you wire a dotenv plugin)
apiUrl=http://127.0.0.1:8000/api/v1
