# Presentation / local demo mode — placeholders only. Never commit real secrets.
# Usage (from apps/backend):
#   copy .env.demo.example .env   (or merge into your local .env)
#   set VOXMETRIKS_SEED_DEMO_ACCOUNTS=1
#   python scripts/seed_integrated_demo.py

DB_PATH=
DEMO_ACCOUNT_PASSWORD=change-me
# Legacy aliases (optional if DEMO_ACCOUNT_PASSWORD is set):
# DEMO_PASSWORD=change-me
# VOXMETRIKS_DEMO_PASSWORD=change-me

VOXMETRIKS_SEED_DEMO_ACCOUNTS=1
EMAIL_PROVIDER=console
MEDIA_STORAGE_ROOT=data/media

# Optional presentation flags
SHOW_DEMO_ORGANIZATIONS=true
ALLOW_DEMO_SELF_APPROVE=true
