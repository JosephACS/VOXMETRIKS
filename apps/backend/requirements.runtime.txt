# ============================================================
# VOXMETRIKS — runtime dependencies only (Docker / production image)
# Versions pinned to match requirements.txt — do not drift arbitrarily.
# Dev tools (pytest, ruff, coverage) stay in requirements.txt.
# ============================================================

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Database
duckdb==1.1.3

# Pydantic v2
pydantic==2.7.4
pydantic-settings==2.3.4

# Environment
python-dotenv==1.0.1

# HTTP client
httpx==0.27.0

# YouTube search fallback
yt-dlp==2024.12.23

# Data processing
pandas==2.2.2
pyarrow==16.1.0
polars==1.14.0

# Logging extras
python-json-logger==2.0.7

# Password hashing
bcrypt==4.1.3
